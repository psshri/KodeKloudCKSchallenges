inside webapp folder

cd webapp
mkdir app
mv app.py /root/webapp/app/app.py
mv requirements.txt /root/webapp/app/requirements.txt
mv templates /root/webapp/app/


groupadd -r user8441 
useradd -r -g user8441 user8441

run the above two commands before docker build

docker build -t kodekloud/webapp-color:stable .

__________

kubesec scan dev-webapp.yaml

vim dev-webapp-mine.yaml
paste the contents

kubesec scan dev-webapp-mine.yaml

kubectl delete pod -n dev dev-webapp

kubectl apply -f dev-webapp-mine.yaml

kubectl get pods -n dev

kubesec scan dev-webapp.yaml

vim staging-webapp-mine.yaml
paste the contents

kubesec scan staging-webapp-mine.yaml

kubectl delete pod -n staging staging-webapp

kubectl apply -f staging-webapp-mine.yaml

kubectl get pods -n staging